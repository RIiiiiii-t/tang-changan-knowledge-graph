# 唐长安小型样例平台导入包人工复核报告 v2

复核日期：2026-08-11  
复核对象：`唐长安小型样例_平台导入包_v1.json`（由小型样例v2重新生成）  
复核范围：对v1人工复核提出的MR-001—MR-007逐项复查。

## 一、复核结论

**结论：人工复核通过，可以进入测试数据库首次导入。**

自动结构校验为0错误、0警告；第一轮人工复核发现的2项P0和5项P1均已解决。本轮未写入平台数据库，首次导入仍须在测试环境执行。

## 二、数据概况

- 来源6条；
- 来源片段29条；
- 实体48个；
- 关系32条；
- FACT实体24个、INTERPRETATION实体23个、POPULAR实体1个；
- 没有悬空关系、无证据关系或自动发布记录。

实体较上一版增加1个，是新增 `TC-ROADNET-0001 唐长安道路网络`，用于承载针对整体路网的研究解释。

## 三、问题关闭情况

### MR-001 已解决：研究结论指向对象

- `TC-CLAIM-0105、0106`改为指向 `TC-ROAD-0001 朱雀大街`；
- 其余8条整体路网结论改为指向 `TC-ROADNET-0001 唐长安道路网络`；
- 没有研究结论继续错误指向 `TC-ROAD-0026 横街`；
- 对应史料片段的 `related_entity_keys` 已同步修正。

### MR-002 已解决：基础实体类型

- 宫城、皇城：`Area`；
- 龙首池：`WaterBody`；
- 代码对照和平台数据契约新增 `Area、WaterBody、RoadNetwork` 及编号规则。

### MR-003 已解决：审核状态依赖

根据项目负责人确认，`TC-CHUNK-1101—1106`原文片段均已审核，现已统一标记为“审核通过”。所有approved对象引用的片段均为approved，不再存在审核依赖倒置。

### MR-004 已解决：科普直接证据元数据

`PopularContent.properties.direct_evidence_links` 已保存片段关联的角色、顺序、公众解释、公开标记和审核状态。`TC-POPLINK-0005`的 `is_visible=false` 得到保留。

### MR-005 已解决：日期格式

科普内容创建和更新日期均转换为ISO格式 `2026-08-11`。

### MR-006 已解决：中文显示标签

研究关系的 `type` 保留英文代码，`label` 改为“阐释、解释、细化”等中文显示名称。

### MR-007 已解决：页码字段

20条带页码的片段均生成 `page_start/page_end`，同时保留原始 `page_label`。`PDF第27页／刊页331`按刊页331生成公开引用页码，并保留完整原始页码说明。

## 四、复核结果

人工复核状态：**通过**  
是否允许测试导入：**是**  
原始审核版v1：**保留未覆盖**  
当前转换输入：**小型样例v2**  
平台数据库：**尚未写入**

## 五、下一步

1. 备份或确认当前青瓷演示数据库可回退；
2. 在测试数据库导入本包；
3. 核对数量、搜索、图谱关系、证据回溯和科普内容显示；
4. 测试通过后再制定正式迁移方案。
